---
// Layoutprimitiv: centrerad max-bredd (skissens 1328px) + responsiv sido-gutter.
interface Props {
  class?: string;
}
const { class: className = "" } = Astro.props;
---

<div
  class:list={[
    "mx-auto w-full max-w-[var(--container-max)] px-[var(--gutter-page)] md:px-10",
    className,
  ]}
>
  <slot />
</div>
